\chapter{Lambda-BLL}\label{chapter:lambll}
\lhead[\fancyplain{}{\bfseries\thepage}]{\fancyplain{}{\bfseries\rightmark}}
This chapter introduces the $\lamBLL$ calculus~\cite{lago2024computationalindistinguishabilitylogicalrelations}, a typed probabilistic lambda calculus for modeling polynomial-time cryptographic computation. Its design and language features capture the essentials for expressing cryptographic experiments, adversaries, and reductions.

$\lamBLL$ provides a programming-language framework in which computational complexity is enforced statically through the type system. In particular, it supports probabilistic polynomial-time computation, bounded interaction with oracles, shared mutable state, and higher-order reasoning about cryptographic constructions.

The type system of $\lamBLL$ is linear and graded, closely related to Bounded Linear Logic \cite{GIRARD19921} and graded calculi \cite{distance-makets-types-stronger}. Resource usage is tracked by polynomial grades, generated by the grammar
\begin{align*}
p ::= 1 \mid i \mid p + p \mid p * p \tag{Polynomials}
\end{align*}
where the variable $i$ represents the security parameter.

The calculus follows a call-by-push-value (CBPV) discipline~\cite{levy2012call}, distinguishing between positive types and general types. This separation permits to restrict function application to positive arguments only.

The base of the type system consists of ground types:
\begin{align*}
\UU \mid \BB \mid \SS[p] 
\end{align*}
representing unit, booleans, and fixed-length bitstrings of polynomially bounded size. These types correspond to the fundamental data manipulated in cryptographic protocols, such as nonces, messages, and keys.

Positive types extend ground types with
\begin{align*}
P \otimes P \mid \bang_p A,
\end{align*}
namely pairs formed with the tensor products $P \otimes P$, and the graded comonadic modality $\bang_p A$. 

In a linear type system, a value must normally be used exactly once. The modality $\bang_p A$ relaxes linearity by allowing a value of type $A$ to be used up to $p$ times. This is central to the cryptographic interpretation of the calculus because it enables a static enforcement of polynomial-time bounds. For example, an oracle of type $\bang_{q(i)} \Oracle$ may be queried at most $q(i)$ times by an adversary for a polynomial $q$. Any attempt to exceed this bound results in an ill-typed term, ensuring that polynomial-time constraints are enforced by typing rather than by operational reasoning. As a result, the proof fails at the type level rather than at runtime.

\begin{figure}[htpb]
  \centering
  \begin{align*}
    G &::= \UU \mid \BB \mid \SS[p] \tag{Ground types} \\
    P &::= G \mid P \otimes P \mid \bang_p A \tag{Positive types} \\
    A &::= P \mid P \multimap A \tag{Types}
  \end{align*}
  \caption{Types of $\lamBLL$.}
  \label{fig:types}
\end{figure}

$\lamBLL$ extends the pure lambda calculus with other features, necessary for modelling cryptographic computation, in particular it defines a collection of function symbols $\mathcal{F}$. Each function symbol $f \in \mathcal{F}$ has a type denoted by $\mathsf{typeof}(f)$ of the form $G_1 \times \dots \times G_m \to G$ where $G_1, \dots, G_m$ and $G$ are ground types. Then, for each polynomial $p$, in $\lamBLL$ there is a corresponding constructor $f_p$ representing a resource-bounded invocation of $f$, with the grade $p$ accounting for its computational cost within the type system.

Values represent data that can be passed, stored or paired but not directly executed, while computations represent effectful execution. Ground values include $\star$, booleans, and bitstrings. Positive values include variables, pairs, and suspended computations of the form $!M$. Computations follow the CBPV structure so values are injected using $\return$, suspended computations are forced using $\der$, and applications is defined only on positive arguments.

\begin{figure}[htpb]
  \centering
  \begin{align*}
    W &::= \star \mid \true \mid \false \mid s \tag{Ground values} \\
    Z &::= x \mid W\mid \lis{Z,Z} \mid \bang M \tag{Positive values} \\
    \Val \ni V &::= Z \mid \lambda x.M \tag{Values} \\
    \Lambda \ni M &::=  \return V \mid  \der (Z) \mid  MZ \tag{Computations} \\
    &\mid \letin{x=N}{M} | \letin{\lis{x,y}=Z}{M} \\
    &\mid \ifthen{Z}{M}{N} \mid \loopp{V}{p}{M} \\
    &\mid \setr{r}{Z} \mid \get r \\
    &\mid f_p(Z_1, \dots, Z,m) \\
  \end{align*}
  \caption{Syntax of $\lamBLL$.}
  \label{fig:syntax}
\end{figure}

To model cryptographic experiments involving shared state, $\lamBLL$ includes global references. References store ground values (and ground only) and are manipulated via $\setr{r}{Z}$ and $\get r$. This ensures that stateful effects do not introduce higher-order behavior beyond what is explicitly admitted by the type system. The language provides also control constructs such as conditionals, let-bindings, and pattern matching on pairs. Bounded iteration is expressed using the construct $\loopp{V}{p}{M}$, which executes the computation $M$ at most $p$ times.

Since variables in $\lamBLL$ are classified according to their types, a variable is said to be ground if its type contains no function arrows, preventing ground terms from encoding higher-order behavior thus being dangerous as global references. To track higher-order dependencies, the calculus distinguishes the set of free higher-order variables of a term $M$, denoted $\mathit{HOFV}(M)$. This set contains exactly those free variables whose types include at least one occurrence of the function type constructor $(\multimap)$.

The typing rules for $\lamBLL$ are presented in Figure~\ref{fig:typingRules} and formalize its type system through a set of rules that assign types to terms while respecting linearity and resource constraints. $\lamBLL$ considers two different typing contexts
\begin{align*}
\Gamma &::= \emptyset \mid \Gamma, x: P \tag{Variable context} \\
\Theta &::= \emptyset \mid \Theta, r: P \tag{Reference context}
\end{align*}
The context $\Gamma$ contains term variables with positive types only, which are not computations, while the reference context $\Theta$ contains references which are can only be ground types. Fixed a reference context $\Theta$ then there are two typing judgments with respect to $\Theta$.
\begin{align*}
\Gamma &\vdash_v^\Theta V: A \tag{Value typing} \\
\Gamma &\vdash_c^\Theta M: A \tag{Computation typing}
\end{align*}
Computations may access references, but references never contain computations. The notation $\pi \triangleright \Gamma \vdashtheta{v} M:A$ and $\pi \triangleright \Gamma \vdashtheta{c} M:A$ indicates that $\pi$ is a type derivation witnessing, respectively, the value or computation typing judgment under variable context $\Gamma$ and reference context $\Theta$. Type derivations are preserved under the substitution of the security parameter with a polynomial which is crucial for modelling polynomial-time computations and for reasoning about families of terms that are indexed by the security parameter.

The polynomial addition induces a binary partial operation $\boxplus$ on positive types defined by induction
\begin{align*}
	G \boxplus G &:= G\\
	(P \otimes Q) \boxplus(R \otimes S) &:= (P \boxplus R) \otimes (Q \boxplus S) \\
	(\bang_p^{\Theta} A) \boxplus (\bang_q^{\Theta}  A) &:= \bang_{p+q}^{\Theta}  A
\end{align*}
The partial operation $\boxplus$ can be extended as a total operation on contexts
\begin{align*}
	\varnothing \boxplus \varnothing&:= \varnothing \\
	(\Gamma, x:P) \boxplus \Delta & := \begin{cases}
		\Gamma, x: P \boxplus \Delta & \text{ if } x \text{ does not occur in } \Delta \\
		x : P \boxplus Q, \Gamma \boxplus \Sigma & \text{ if } \Delta = \Sigma, x: Q
	\end{cases}
\end{align*}

And to account for polynomial multiplication, for every polynomial $p \in \mathbb{N}_{\geq 1}[i]$ there is a total unary operation on positive types by induction:
\begin{align*}
	p * G &:= G\\
	p * (P \otimes Q)  &:= (p * P) \otimes (p * Q) \\
	p * (\bang_q^{\Theta}  A) &:= \bang_{p\times q}^{ \Theta}  A
\end{align*}

The operation $p * (-)$ on positive types can also be extended to a total operation on term variables contexts:
\[\begin{aligned}
	p \ast \varnothing &:= \varnothing\\
	p \ast  (x : P,\Gamma )&:= (x : p \ast P),  p\ast \Gamma 
\end{aligned}\]

The notion of substitution in $\lamBLL$ is defined with respect to the security parameter $i$. Given a polynomial $p \in \mathbb{N}_{\geq 1}[i]$ and a type $A$, the type $Ap$ is the type obtained by substituting every occurrence of the parameter $i$ in $A$ with the polynomial $p$, that is, $A[p/i]$. Similarly, for a term $M$, the type $Mp$ to denote the term obtained by substituting $p$ for $i$ in all polynomial annotations occurring in $M$, namely $M[p/i]$. 

\newcommand{\subst}[1]{#1}
\begin{figure}[ht]
\vspace*{-6mm}
\begin{center} \scriptsize
\begin{mathpar}\mprset{sep=1em,  vskip =-0.1ex}
		\inferrule* [right=\scriptsize var]{\\}{ x : P \vdashtheta{v} x: P}
		\and 
		\inferrule* [right=\scriptsize true]{\\}{\vdashtheta{v} \true : \BB}	
		\and
		\inferrule* [right=\scriptsize false]{\\}{\vdashtheta{v} \false: \BB}	
		\and
		\inferrule* [right=\scriptsize fun]{ \mathsf{typeof}(f) = G_1 \times \dots \times G_m \to G \\ (\Gamma_k\subst{p} \vdashtheta{v} Z_k : G_k\subst{p})_{1\leq k \leq m} \\ p \in \N_{\geq 1}[i]}{ \boxplus_k \Gamma_k\subst{p} ; \Theta \vdashtheta{c} f_p(Z_1, \dots, Z_m):G\subst{p}}
		\and
		\inferrule* [right=\scriptsize string]{s\in \{0,1\}^{c} \\ p : i \mapsto c\text{ is a constant polynomial}}{\Gamma \vdashtheta{v} s : \SS[p]}	
		\and
		\inferrule* [right=\scriptsize tensor]{\Gamma \vdashtheta{v} Z_1 : P \\ \Delta \vdashtheta{v} Z_2: Q}{\Gamma \boxplus \Delta \vdashtheta{v} \lis{Z_1, Z_2} : P\otimes Q}	
		\and
		\inferrule* [right=\scriptsize let-pair]{\Gamma \vdashtheta{v} Z : P \otimes Q \\ \Delta, x : P, y : Q \vdashtheta{c}  M : A}{\Gamma \boxplus \Delta \vdashtheta{c} \letin{ \lis{x,y} = Z}{M} : A }
		\and
        \inferrule* [right=\scriptsize unit]{\\}{\vdashtheta{v} \star : \UU}
        \and
        \inferrule* [right=\scriptsize bang]{\Gamma \vdashtheta{c} M : A}{p \ast \Gamma \vdashtheta{v} \bang M : \bang_p^{\Theta} A}
		\and
		\inferrule* [right=\scriptsize der]{\Gamma \vdashtheta{v} Z : \bang_q^{\Theta} A }{\Gamma \vdashtheta{c} \der (Z):A } 	
		\and
		\inferrule* [right=\scriptsize app]{\Gamma \vdashtheta{c} M : P\arrowRef{\Theta} A
		\\ \Delta \vdashtheta{v} Z : P}{\Gamma\boxplus \Delta \vdashtheta{c} MZ : A}	
		\and	
        \inferrule* [right=\scriptsize lam]{\Gamma, x: P \vdashtheta{c} M : A }{\Gamma \vdashtheta{v} \lambda x.M : P \multimap A}
        \and
		\inferrule* [right=\scriptsize eta]{\Gamma \vdashtheta{v} V: A}{\Gamma \vdashtheta{c} \return V: A}
        \and
		\inferrule* [right=\scriptsize let]{\Gamma \vdashtheta{v} Z :  P \otimes Q \\ \Delta, x : P, y : Q \vdashtheta{c} M : A}{\Gamma \boxplus \Delta \vdashtheta{c} \letin{ \lis{x,y} = Z}{M} : A }
		\and
		\inferrule* [right=\scriptsize loop]{\Gamma \vdashtheta{v} V: P \multimap P \\ \Delta \vdashtheta{c} M : P }{(p \ast \Gamma)\boxplus \Delta \vdashtheta{c} \loopp{V}{p}{M} : P }	
		\and
		\inferrule* [right=\scriptsize set]{\Gamma \vdashtheta{v} Z: G \\ r:G \in \Theta}{\Gamma \vdashtheta{c} \setr{r}{Z} : \UU }	
		\and 
		\inferrule* [right=\scriptsize get]{ r:G \in \Theta}{ \vdashtheta{c} \get r: G}
		\and 
        \inferrule* [right=\scriptsize case]{\Gamma \vdashtheta{v} Z: \BB \\ \Delta \vdashtheta{c} M : A \\ \Delta \vdashtheta{c} N : A}{\Gamma \boxplus \Delta \vdashtheta{c} \ifthen{Z}{M}{N} : A}
        \and
        \inferrule*[right=\scriptsize weak]{\Gamma \vdashtheta{v}V: A \\ x \ \text{not free in} \ \Gamma}{\Gamma, x: P \vdashtheta{v} V:A}
        \and
        \inferrule*[right=\scriptsize weak]{\Gamma \vdashtheta{c}M: A \\ x \ \text{not free in} \ \Gamma}{\Gamma, x: P \vdashtheta{c} M:A}
\end{mathpar}
\end{center}
\vspace*{-6mm}
\caption{Typing rules of $\lamBLL$.}
\label{fig:typingRules}
\end{figure}

A computation in $\lamBLL$ can be probabilistic and can read or modify the global reference context. In order to model probabilistic effects together with mutable references, $\lamBLL$ combines the distribution monad with the state monad.

To account for probabilistic effects, $\lamBLL$ models probabilistic computation using finite probability distributions. Given a set $X$, a probability distribution over $X$ is a function $\mu : X \to [0,1]$ with finite support and total mass equal to $1$. Any such distribution can be written as a finite weighted sum of Dirac distributions $\delta_x$, which represent deterministic outcomes. This construction induces a monad $(\D, \eta_{\D}, \bind_{\D})$ on the category $\mathbf{Set}$ of sets and functions. The unit maps a value $x$ to the Dirac distribution $\delta_x$, embedding pure values into probabilistic computations having components $\eta_X : x \mapsto \delta_x$, while the bind operation models sequential probabilistic computation by combining distributions:
\[
\bind : \D(X) \times \mathbf{Set}(X,\D(Y)) \to \D(Y).
\]
Here, $\D(X)$ denotes the set of all probability distributions over $X$.

To model mutable references, $\lamBLL$ uses a state monad whose state space is given by the set
of stores associated with a fixed reference context $\Theta$. A store assigns to each reference
a ground value of the appropriate type, thereby ensuring that memory remains well-typed
throughout execution. Reading a reference retrieves its current value from the store, while
writing to a reference produces a new store in which the corresponding location is updated.

The full semantics of computations in $\lamBLL$ is obtained by combining probabilistic choice with state. For a fixed reference context $\Theta$, computations returning values in a set $X$ are interpreted as functions from stores to probability distributions over pairs of values and stores. For every closed reference context $\Theta$, there is a corresponding monad $(\T_\Theta, \eta_\Theta, \bind_\Theta)$ on $\Set$, defined as the tensor product of the distribution monad with the state monad, namely
\[
\T_{\Theta} := \bigl(\D(X \times \Store_{\Theta})\bigr)^{\Store_{\Theta}}
\]
The unit of $\T_\Theta$ has components $\eta_\Theta : X \to \D(X \times \Store_\Theta)^{\Store_\Theta}$, mapping a value $x \in X$ and a store $e \in \Store_\Theta$ to the Dirac distribution $\delta_{(x,e)}$, corresponding to a deterministic computation that returns $x$ without
modifying the store. The bind operator
\[
\bind_\Theta : \T_\Theta X \times \mathbf{Set}(X, \T_\Theta Y) \to \T_\Theta Y
\]
maps $\varphi \in \T_\Theta X$ and $f : X \to \T_\Theta Y$ to the function
$\dlambda e.\, \bigl(\varphi(e) \bind_\D \eval \circ (f \times \id_{\Store_\Theta})\bigr)$,
where $\dlambda$ denotes currying and $\eval$ is the evaluation map induced by the Cartesian closed structure of $\Set$.

In $\lamBLL$, application is written as $MZ$, and the constructor $\return$ lifts values into computations. The syntactic sugar
\begin{align}
VZ \triangleq (\return V)\,Z \label{eq:sugar-return}
\end{align}
simplifies expressions by implicitly promoting values to computations, reducing the need for nested $\return$ constructs. Moreover, the syntactic sugar
\begin{align}
M;N \triangleq \letin{\_ = M}{N} \label{eq:sugar-letvoid}
\end{align}
is used to compose effectful computations, such as the $\mathtt{set}$ command, without binding a variable. Finally, the sugar
\begin{align}
\mathtt{skip} \triangleq \return \star \label{eq:sugar-skip}
\end{align}
denotes a computation that performs no effect.

\section{Useful Functional Symbols and Equations}\label{sec:useful-functional-symbols-and-equations}
\paragraph{Functional Symbols}
We introduce a collection of function symbols for $\lamBLL$. These symbols capture fundamental computational patterns such as randomness, bitstring manipulation, and memory operations and they extend the set of function symbols presented in \cite{lago2024computationalindistinguishabilitylogicalrelations}, which are recalled in Appendix~\ref{app:1}, Table~\ref{table:function-symbols}.

Table~\ref{table:new-function-symbols} presents the new function symbols, for clarity they are organized into two categories according to their intended purpose.

\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.0}
\begin{footnotesize}
\begin{xltabular}{\textwidth}{|@{\hskip 1.5pt}c|c|@{\hskip 1.5pt}X@{\hskip 1.5pt}|}
    \hline
\textbf{Function Symbol} & \multicolumn{1}{c|}{\textbf{Type}} & \multicolumn{1}{c|}{\textbf{Interpretation}} \\ \hline
\multicolumn{3}{|c|}{\textbf{Function Symbols for Manipulating Bitstrings }} \\ \hline

$\mathtt{eq}$ & $\SS[i]\times\SS[i]\to\BB$ & Function that takes as input two strings of length $i$, and returns true if they are equals and false otherwise. \\ \hline

\multicolumn{3}{|c|}{\textbf{Function Symbols for Ledger Manipulation}} \\ \hline

$\mathtt{initQ}_q$ & $\SS[q\times(i+1)]$ & Function that initializes a  query ledger by generating a string of length $q\times(i+1)$ filled entirely with zeros. \\ \hline

$\mathtt{contains}_q$ & $\SS[q \times (i+1)] \times \SS[i] \to \BB$ & Function that, given a query ledger and an input string of length $i$, searches for a row in the ledger whose first $i$ bits are equal to the input string and whose final bit is set to 1 and returns a boolean indicating whether the substring was found or not. \\ \hline

$\mathtt{modify}_q$ &
$\begin{aligned}[t]
    &\SS[q\times(i+1)] \times \SS[i] \times \SS[i] \\ 
    &\to \SS[q\times(i+1)]
\end{aligned}$ & 
Function that, given a query ledger and two input strings of length $i$, identifies the first inactive row in the ledger, inserts into that row the concatenation of the two strings followed by a bit set to 1 (indicating activation), and returns the resulting updated ledger. \\ \hline

$\mathtt{coherent}_{q,p}$ &
$\begin{aligned}[t]
    &\SS[q \times (i+1)] \times \SS[p \times (2i + 1)] \\
    &\to \BB
\end{aligned}$ &  
Function that, given two ledgers, it verifies that every message present in the first ledger exists as a key in the the second ledger, and conversely, that no keys exist in the second ledger that are not also recorded in the first one. \\ \hline  

\caption{Table of function symbols and their interpretations. We consider ledgers modelled by strings of length $(q\times(i+1))$ and $(p\times(2i+1))$, where $p$ and $q$ are polynomials in $\mathbb{N}_{\geq1}[i]$. This string represents a table with $q$ (resp., $p$) rows, where each row consists of two strings of length $i$ (resp., $2i$) followed by a single bit, which is set to 1 if the corresponding row is active, and 0 otherwise.}
\label{table:new-function-symbols}
\end{xltabular}
\end{footnotesize}

The first group of function symbols concerns operations on bitstrings. In particular, this group contains only the function symbol $\mathtt{eq}$, which denotes a function that, given two bitstrings of length $i$, returns a boolean value indicating whether they are equal.

The second group consists of function symbols used to interact with a ledger, a structure that behaves as a finite table. The ledger is represented as a flat binary string segmented into rows of length $i+1$. Each row is composed of a bitstring of length $i$ together with an additional bit that indicates whether the row is active (set to $1$) or inactive (set to $0$). The total size of the ledger is chosen to fit $q$ such rows, where $q$ is a polynomial in $\mathbb{N}_{\geq 1}[i]$. The number of rows must be polynomially bounded otherwise the resulting string could exceed the size constraints imposed by $\lamBLL$.

The function symbol $\mathtt{initQ}$ constructs an empty ledger by returning a bitstring initialized entirely with zeros, thus representing a table with no active entries. The symbol $\mathtt{contains}$ checks whether a given input string is already present in the ledger. It scans the ledger to determine whether there exists a row whose first $i$ bits match the input string and whose final bit is set to $1$. If such a row is found, the input string is considered to be defined in the ledger and the function returns $\true$. The function symbol $\mathtt{modifyQ}$ enables the insertion of a new string into the ledger: it searches for the first inactive row, writes the string into the first $i$ positions, and sets the final bit to $1$, thereby marking the row as active. Finally, the function symbol $\mathtt{coherent}$ checks whether every active entry in a ledger of size $q \times (i+1)$ also appears as an active entry in a ledger of size $p \times (2i+1)$. More precisely, for every row in the first ledger whose last bit is set to $1$, there exists a corresponding active row in the second ledger such that the first $i$ bits of its $2i$-bit payload coincide with the $i$-bit string stored in the first ledger ensuring that all bitstrings recorded as active in the first ledger are consistently reflected in the second one.

\paragraph{Equations}
These equations extend the set introduced in \cite{lago2024computationalindistinguishabilitylogicalrelations}. For completeness, these equations are recalled in Appendix~\ref{app:1}, Figure~\ref{fig:equations}. They serve as semantic constraints specifying how the function symbols behave under evaluation.

\begin{figure}[htpb]
    \centering
    \begin{align*}
        \letin{x=\random}{\mathtt{eq}(x,t)} \simcontext \return \false \tag{randF}\label{randF} 
        \\[2ex]
        \left(\begin{aligned}
            &\mathtt{if}\,b\,\mathtt{then}\,\return v \\
            &\mathtt{else}\,\return v
        \end{aligned}\right) \simcontext \return v \tag{ifConst}\label{ifConst}
    \end{align*}
\end{figure}

Equation~\ref{randF} formalizes a recurring pattern in which a freshly sampled random value is compared with an independently provided value. In this setting, a fresh random variable $x$ is generated via the primitive $\random$ and is subsequently compared with some value $t$ using the equality predicate $\mathtt{eq}$. Because $x$ is drawn uniformly at random and independently of all values already contained in the program state, the probability that $x$ coincides with the fixed value $t$ is negligible. Consequently, the equality test $\mathtt{eq}(x,t)$ evaluates to $\false$ except with negligible probability. This equation plays a central role in the analysis of cryptographic constructions. For example, when an adversary attempts to guess a randomly generated nonce or challenge, the probability that the adversary's guess equals the freshly sampled value is negligible. Equation~\ref{randF} will therefore be a key ingredient in the security proof of existential unforgeability under chosen-message attacks for PRF-induced MACs.

Equation~\ref{ifConst} formalizes a simple simplification pattern for conditional expressions. In this equation, both branches of the conditional expression evaluate to the same value $v$. Consequently, the overall behavior of the program is invariant with respect to the truth value of the boolean guard $b$: whether $b$ evaluates to $\true$ or $\false$, the result is always $v$. Therefore, within any program context, the conditional expression is observationally equivalent to the expression $v$ itself. This equation is particularly useful in the simplification of programs generated during program transformations, where conditional constructs may be introduced and subsequently found to produce identical results in both branches.

% A computation in $\lamBLL$ can be probabilistic, and read or modify the global reference context. In order to model probabilistic effects with references $\lamBLL$ uses the distribution monad and the state monad. The idea is that the result of a computation is not just a value, it depends on the initial store, it produces a distribution of outcomes, and each outcome comes with an updated store. Computations are interpreted as functions that accepts the initial store and produce outcoomes as probability distribution over (value, store) pairs.

% To add the effect of probabilistic compuations $\lamBLL$ models probabilistic computation using finite probability distributions. Given a set $X$, a probability distribution over $X$ is a function $\mu : X \to [0,1]$ with finite support and total mass equal to $1$. Any such distribution can be written as a finite weighted sum of Dirac distributions $\delta_x$, which represent deterministic outcomes. This construction induces a monad $(\D, \eta_{\D}, \bind_{\D})$ on the category $\mathbf{Set}$ of sets and functions. The unit maps a value $x$ to the Dirac distribution $\delta_x$, namely $\eta_X : x \mapsto \delta_x$ hence embedding pure values into probabilistic computations, while the bind operation models sequential probabilistic computation by combining distributions according to their weights:
% \[
% \bind : \D(X) \times \mathbf{Set}(X,\D(Y)) \to \D(Y).
% \]
% Here, $\D(X)$ denotes the set of all probability distributions over $X$. As a simple example let's consider a fair coin flip. 
% Let $X = \{\mathsf{H}, \mathsf{T}\}$ and define the distribution
% \[
% \mu = \tfrac{1}{2}\,\delta_{\mathsf{H}} + \tfrac{1}{2}\,\delta_{\mathsf{T}} \in \D(X)
% \]
% Now suppose that each outcome determines a probabilistic computation over a set $Y$, for instance there is a function $f: X \to \D(Y)$ such that
% \[
% f(\mathsf{H}) = \delta_{0}
% \qquad\text{and}\qquad
% f(\mathsf{T}) = \tfrac{1}{2}\,\delta_{0} + \tfrac{1}{2}\,\delta_{1}
% \]
% The bind operation combines these into a single distribution over $Y$:
% \[
% \mu \bind f
% = \tfrac{1}{2}\,\delta_{0}
% + \tfrac{1}{2}\bigl(\tfrac{1}{2}\,\delta_{0} + \tfrac{1}{2}\,\delta_{1}\bigr)
% = \tfrac{3}{4}\,\delta_{0} + \tfrac{1}{4}\,\delta_{1}.
% \]
% Sequential probabilistic choices are composed by propagating and weighting probabilities. The probabilities from the first distribution are pushed forward through the second.

\section{Hoare Logic}\label{sec:extensions-lambda-bll}

We introduce a Hoare logic~\cite{axiomatic-basis-for-computer-programming}, which will be fundamental to formalize the proof in Chapter~\ref{chapter:proof} of  existential unforgeability under chosen-message attacks for PRF-induced MACs in the $\lamBLL$ framework. 

Following Hoare's original formalism, program behavior is specified by \emph{partial correctness specifications}, written as triples of the form
\[
\{P\}\, e \,\{Q\} \footnote{Actually in the original paper the notation is $P\,\{Q\}\, R$ and has to be interpreted as \enquote{if the assertion $P$ is true before
initiation of a program $Q$, then the assertion $R$ will be
true on its completion}}.
\]
Here, $e$ denotes a program, and $P$ and $Q$ are logical propositions representing the precondition and postcondition, respectively. The intended meaning of such a triple is that, if execution of $e$ starts in a program state satisfying $P$, then every terminating execution of $e$ produces a state satisfying $Q$.

A Hoare triple of the form \(\{P\}\, e \,\{\lambda v.\, Q(v)\}\) is referred to as a partial correctness specification. It is termed partial because, if the execution of \(e\) diverges, the triple imposes no additional constraints and does not assert any property about the non-terminating behavior. A stronger form of specification is a total correctness specification, which asserts that whenever \(e\) is executed in a state satisfying \(P\), the execution of \(e\) is guaranteed to terminate and, upon termination, the postcondition \(Q\) holds. Since \(\lamBLL\) is a strongly normalizing calculus, we adopt the standard notation for partial correctness Hoare triples using curly braces.

Since \(Q\) is a predicate on values namely, on the value to which \(e\) evaluates, we write the Hoare triple \(\{P\}\, e \,\{\lambda v.\, Q(v)\}\), where \(v\) denotes the result of evaluating \(e\). When a postcondition does not depend on the value returned by the expression, we write $ \{P\}\, e \,\{\lambda \_.\, Q\}$ denoting that the result of the computation $e$ is ignored.michi

Propositions used in Hoare triples are generated by the following grammar:
\begin{align*}
\Phi ::= \phi \mid \Phi \land \Phi \mid \Phi \lor \Phi
\end{align*}
where $\phi$ ranges over atomic first-order formulas such as $\true$, $\false$, $t_1 = t_2$, $t_1 \in t_2$, or $\mathtt{coherent}(t_1, t_2)$. These formulas constitute the assertion language of the logic, while semantic implication $P \models Q$ is defined at the meta-level.

Given a typing judgment of the form $\vdashtheta{c}\, e$, propositions are restricted to ground values that appear in $e$ or in $\Theta$, and possibly to the result of the computation $e$ when it has ground type.

Using the notion of Hoare triples, we introduce a new set of typing rules for $\lamBLL$, presented in Figure~\ref{fig:hl-typing-rules}. These rules assign types to terms while respecting linearity and resource constraints, and they equip $\lamBLL$ with a simple Hoare logic.

\begin{figure}[htpb]
\vspace*{-6mm}
\begin{center} \footnotesize
\begin{mathpar}\mprset{sep=1em,  vskip =-0.1ex}
    \inferrule* [right=\scriptsize \labelrule{h-eta}{rule:h-eta}]{\Gamma \vdashtheta{v} V: A}{\Gamma \vdashtheta{c} \{P[V/v]\}\,\return V \,\{\lambda v.P(v)\}: A}
    \and
    \inferrule* [right=\scriptsize \labelrule{h-bang}{rule:h-bang}]{ \Gamma \vdashtheta{c} \{P\} M \{\lambda v. Q(v)\}: A \\ Q \models P}{ p \ast \Gamma \vdashtheta{v} \bang M: \bang_p^{P, Q} A}
    \and
    \inferrule* [right=\scriptsize \labelrule{h-app}{rule:h-app}]{ \Gamma \vdashtheta{c} \{P\} M \{\lambda f. Q(f)\} : A \underset{R, S}{\multimap} B \\ \Delta \vdashtheta{v} Z : A \\ Q \models R }{ \Gamma \boxplus \Delta \vdashtheta{c} \{P\} \, MZ \, \{\lambda v. S(v)\} : B }
    \and
    \inferrule* [right=\scriptsize \labelrule{H-LAM}{rule:h-lam}]{\Gamma, x: A \vdashtheta{c} \{P(x)\}\,M\,\{\lambda v.Q(x, v)\}: B}{\Gamma \vdashtheta{v} \lambda x . M : A \underset{\lambda x.P(x), \lambda x.\lambda v.Q(x,v)}{\multimap} B }
    \and
    \inferrule* [right=\scriptsize \labelrule{h-fun}{rule:h-fun}]{\mathsf{typeof}(f)=G_1\times\dots\times G_m \to G \\ (\Gamma_{k}p \vdashtheta{v} Z_k: G_{k}p)_{1\leq k\leq m} \\ p \in \mathbb{N}_{\geq 1}[i]}{\boxplus_k \Gamma_k \vdashtheta{c}\{P[f(Z_1,\dots, Z_m)/v]\}\,f_p(Z_1, \dots, Z_m)\,\{\lambda v.P(v)\}: Gp}
    \and
    \inferrule* [right=\scriptsize \labelrule{h-get}{rule:h-get}]{r:G\in \Theta}{\Gamma \vdashtheta{c} \{P(r)\}\,\get r \,\{\lambda x.P(x)\}: G}
    \and
    \inferrule* [right=\scriptsize \labelrule{h-set}{rule:h-set}]{\Gamma \vdashtheta{v} Z: G \\ r:G\in \Theta}{ \Gamma \vdashtheta{c} \{P[Z/r]\}\, \setr{r}{Z} \,\{P\}: \UU }
    \and
    \inferrule* [right=\scriptsize \labelrule{h-let}{rule:h-let}]{\Gamma \vdashtheta{c} \{P\}\, M \,\{\lambda v.Q(v)\}:A \\ \Delta, x:A \vdashtheta{c} \{Q(x)\}\, N \,\{S\}: B}{\Gamma \boxplus \Delta \vdashtheta{c} \{P\}\, \letin{x=M}{N} \,\{S\}: B}
    \and
    \inferrule* [right=\scriptsize \labelrule{h-case}{rule:h-case}]{\Gamma \vdashtheta{v} Z: \BB \\ \Delta \vdashtheta{c} \{P \land (Z=\true)\}\, M \,\{R\} : A \\ \Delta \vdashtheta{c} \{P \land (Z=\false)\}\, N \,\{R\} : A}{\Gamma \boxplus \Delta \vdashtheta{c} \{P\}\, \ifthen{Z}{M}{N} \,\{R\} : A}
    \and
    \inferrule* [right=\scriptsize \labelrule{h-cons}{rule:h-cons}]{P' \models P \quad Q \models Q' \\ \Gamma \vdashtheta{c} \{P\}\, M \,\{\lambda v.Q(v)\}: A }{\Gamma \vdashtheta{c} \{P'\}\, M \,\{\lambda v.Q'(v)\} : A}
    % \and
    % \inferrule*[right=h-inv]{\Gamma;\Theta \vdash \{P\}\,M\,\{\lambda v.Q(v)\} \\ \forall v.I\land Q(v) \implies I}{\Gamma;\Theta \vdash \{P \land I\}\,M\,\{\lambda v.Q(v) \land I\}}
\end{mathpar}
\end{center}
\vspace*{-6mm}
\caption{Typing rules of $\lamBLL$ extended with Hoare Logic.}
\label{fig:hl-typing-rules}
\end{figure}

To relate the original definition of $\lamBLL$ with its extension equipped with Hoare logic, we define a translation from standard types to types decorated with propositions. Given a standard type~$A$ and an invariant~$P$, the lifting $\mu$ produces a decorated type in which~$P$ is threaded through every computation. The map $\mu$ is defined inductively as follows:
\begin{align}
\begin{split}
&\mu(\UU, P) = \UU \label{eq:mu} \\
&\mu(\BB, P) = \BB \\
&\mu(\SS[p], P) = \SS[p] \\
&\mu(A \otimes B, P) = \mu(A, P) \otimes \mu(B, P) \\
&\mu(\bang_q A, P) = \bang_q^{P,P} \mu(A, P) \\
&\mu(A \multimap B, P) = \mu(A, P) \underset{P, \lambda x.P}{\multimap} \mu(B, P)
\end{split}    
\end{align}
We now state a lemma that lifts a typing judgment to a Hoare typing judgment.
\begin{lemma}\label{lemma:hoare-lifting}
Let $P$ be a proposition over a set of references $\mathcal{R}(P)$.
If $\Gamma \vdash^{\Theta} M : A$ then $\mu(\Gamma, P) \vdash^{\Xi} M: \mu(A, P)$ whenever $\Xi \supseteq \Theta$ and $\mathcal{R}(P) \cap \Theta = \emptyset$.
\end{lemma}
\begin{proof}
The proof proceeds by induction on the derivation of $\Gamma \vdash^{\Theta} M : A$.  
\end{proof}
% \begin{proof}
% The proof proceeds by induction on the derivation of $\Gamma \vdash^{\Theta} M : A$.

% \begin{itemize}
%     \item Rule VAR:
%     \begin{mathpar}
%     \inferrule*[right=var]{ }{x : A \vdash^{\Theta}_{v} x : A}
%     \end{mathpar}
%     By definition of the lifting $\mu$, a variable declaration
%     $x : A$ in the standard system is translated to
%     $x : \mu(A,P)$ in the decorated system. Hence, by a direct
%     application of Rule VAR in the Hoare system, we obtain
%     \[
%         x : \mu(A,P) \vdash^{\Xi}_{v} x : \mu(A,P).
%     \]

%     \item Rule TRUE:
%     \begin{mathpar}
%     \inferrule*[right=true]{ }{ \vdash^{\Theta}_{v} \true : \BB }
%     \end{mathpar}
%     Since $\mu(\BB,P)=\BB$, the rule is unchanged by the lifting.
%     Hence, by Rule TRUE in the decorated system,
%     \[
%         \vdash^{\Xi}_{v} \true : \BB .
%     \]

%     \item Rule FALSE:
%     \begin{mathpar}
%     \inferrule*[right=false]{ }{ \vdash^{\Theta}_{v} \false : \BB }
%     \end{mathpar}
%     Again, $\mu(\BB,P)=\BB$, so the rule is preserved.
%     Thus,
%     \[
%         \vdash^{\Xi}_{v} \false : \BB .
%     \]

%     \item Rule UNIT:
%     \begin{mathpar}
%     \inferrule*[right=unit]{ }{ \vdash^{\Theta}_{v} \star : \UU }
%     \end{mathpar}
%     Since $\mu(\UU,P)=\UU$, we directly obtain
%     \[
%         \vdash^{\Xi}_{v} \star : \UU .
%     \]

%     \item Rule STRING:
%     \begin{mathpar}
%     \inferrule*[right=string]{s\in\{0,1\}^c \and p:i\mapsto c}{\Gamma \vdashtheta{v} s : \SS[p]}
%     \end{mathpar}
%     Since $\mu(\SS[p],P)=\SS[p]$, the lifting does not affect
%     string types. Therefore, by a direct application of Rule STRING in the decorated system, we obtain
%     \[ \mu(\Gamma,P) \vdashtheta{v} s: \SS[p]. \]

%     \item Rule TENSOR:
%     \begin{mathpar}
%     \inferrule*[right=tensor]
%     {\Gamma \vdash^{\Theta}_{v} Z_1 : A
%      \quad
%      \Delta \vdash^{\Theta}_{v} Z_2 : B}
%     {\Gamma \boxplus \Delta \vdash^{\Theta}_{v} \langle Z_1,Z_2\rangle : A \otimes B}
%     \end{mathpar}
%     By the induction hypothesis,
%     \[
%         \mu(\Gamma,P) \vdash^{\Xi}_{v} Z_1 : \mu(A,P), \qquad
%         \mu(\Delta,P) \vdash^{\Xi}_{v} Z_2 : \mu(B,P).
%     \]
%     Since $\mu(A\otimes B,P)=\mu(A,P)\otimes\mu(B,P)$,
%     an application of Rule TENSOR in the decorated system yields
%     \[
%         \mu(\Gamma,P)\boxplus\mu(\Delta,P) \vdash^{\Xi}_{v} \lis{Z_1, Z_2}:\mu(A,P)\otimes\mu(B,P).
%     \]

%     \item Rule LAM:
%     \begin{mathpar}
%     \inferrule*[right=lam]
%     {\Gamma,x:A \vdash^{\Theta}_{c} M : B}
%     {\Gamma \vdash^{\Theta}_{v} \lambda x.M : A \multimap B}
%     \end{mathpar}
%     By the induction hypothesis,
%     \[
%         \mu(\Gamma,P),x:\mu(A,P) \vdash^{\Xi}_{c} \{P\}\,M\,\{\lambda v.P\} : \mu(B,P).
%     \]
%     Applying Rule H-LAM yields
%     \[
%         \mu(\Gamma,P) \vdash^{\Xi}_{v} \lambda x.M : \mu(A,P) \underset{P,\lambda x.P}{\multimap} \mu(B,P).
%     \]
%     By definition of $\mu$, this type is $\mu(A\multimap B,P)$.

%     \item Rule APP:
%     \begin{mathpar}
%         \inferrule*[right=app]{\Gamma \vdash^{\Theta}_{c} M : A \multimap B
%          \and \Delta \vdash^{\Theta}_{v} Z : A}{\Gamma\boxplus\Delta \vdash^{\Theta}_{c} MZ : B}
%     \end{mathpar}
%     By the induction hypothesis we have
%     \[ \mu(\Gamma,P) \vdash^{\Xi}_{c} \{P\}\, M \,\{\lambda f.P\} : \mu(A,P)\underset{P,\lambda x.P}{\multimap}\mu(B,P) \]
%     and
%     \[
%         \mu(\Delta,P) \vdash^{\Xi}_{v} Z:\mu(A,P)
%     \]
%     Since $P \vdash P$ holds trivially, Rule H-APP yields
%     \[
%         \mu(\Gamma,P)\boxplus\mu(\Delta,P) \vdash^{\Xi}_{c} \{P\}\, MZ \,\{\lambda v.P\} : \mu(B,P).
%     \]

%     \item Rule ETA:
%     \begin{mathpar}
%     \inferrule*[right=eta]{\Gamma \vdash^{\Theta}_{v} V : A}{\Gamma \vdash^{\Theta}_{c} \return V : A}
%     \end{mathpar}
%     By induction hypothesis,
%     \[
%         \mu(\Gamma,P) \vdash^{\Xi}_{v} V : \mu(A,P).
%     \]
%     Applying Rule H-ETA gives
%     \[
%     \mu(\Gamma, P) \vdash^{\Xi}_{c} \{P\}\,\return V\,\{\lambda v.P\}: \mu(A,P).
%     \]

%     \item Rule BANG:
%     \begin{mathpar}
%     \inferrule*[right=bang]{\Gamma \vdash^{\Theta}_{c} M : A}{p * \Gamma \vdash^{\Theta}_{v} \bang M : \bang_p A}
%     \end{mathpar}
%     By induction hypothesis,
%     \[
%         \mu(\Gamma,P) \vdash^{\Xi}_{c} \{P\}\, M \,\{\lambda v.P\}: \mu(A,P).
%     \]
%     Since $P \vdash P$, Rule H-BANG yields
%     \[
%     p * \mu(\Gamma,P)
%     \vdash^{\Xi}_{v}
%     \bang M
%     :
%     \bang_p^{P,P}\mu(A,P),
%     \]
%     which equals $\mu(\bang_p A,P)$ by definition.

%     \item Rule LET:
%     \begin{mathpar}
%     \inferrule*[right=let]{\Gamma \vdash^{\Theta}_{c} M : A \and \Delta,x:A \vdash^{\Theta}_{c} N : B}{\Gamma\boxplus\Delta \vdash^{\Theta}_{c} \letin{x=M}{N} : B}
%     \end{mathpar}
%     By induction hypothesis,
%     \[ \mu(\Gamma,P) \vdash^{\Xi}_{c} \{P\}\, M \,\{\lambda v.P\} \]
%     and
%     \[ \mu(\Delta,P),x:\mu(A,P) \vdash^{\Xi}_{c} \{P\}\, N \,\{\lambda v.P\}.\]
%     Rule H-LET yields
%     \[
%         \mu(\Gamma,P)\boxplus\mu(\Delta,P) \vdash^{\Xi}_{c} \{P\}\, \letin{x=M}{N} \,\{\lambda v.P\} : \mu(B,P).
%     \]
    
%     \item Rule LET\_PAIR:
%     \begin{mathpar}
%     \inferrule*[right=let\_pair]{\Gamma \vdash^{\Theta}_{v} Z : A \otimes B \and \Delta,x:A,y:B \vdash^{\Theta}_{c} M : C}{\Gamma\boxplus\Delta \vdash^{\Theta}_{c} \letin{\lis{x,y}=Z}{N} : C}
%     \end{mathpar}
%     By induction hypothesis,
%     \[ \mu(\Gamma,P) \vdash^{\Xi}_{v} \mu(A, P) \otimes \mu(B, P) \]
%     and
%     \[ \mu(\Delta,P),x:\mu(A,P), y:\mu(B,P) \vdash^{\Xi}_{c} \{P\}\, M \,\{\lambda v.P\}.\]
%     Rule H-LET\_PAIR is not explicitly defined in our system. However, assuming a standard definition, analogous to the usual LET rule but binding a tuple of variables, we obtain the following:
%     \[
%         \mu(\Gamma,P)\boxplus\mu(\Delta,P) \vdash^{\Xi}_{c} \{P\}\, \letin{\lis{x,y}=Z}{M} \,\{\lambda v.P\} : \mu(C,P).
%     \]

%     \item Rule GET:
%     \begin{mathpar}
%     \inferrule*[right=get]{r:A \in \Theta}{\vdash^{\Theta}_{c} \get r : A}
%     \end{mathpar}
%     Since $\mathcal R(P)\cap\Theta=\emptyset$, $P$ does not depend on $r$.
%     Hence Rule H-GET yields
%     \[ \vdash^{\Xi}_{c} \{P\}\, \get r \,\{\lambda v.P\} : \mu(A,P). \]

%     \item Rule SET:
%     \begin{mathpar}
%     \inferrule*[right=set]{\Gamma \vdash^{\Theta}_{v} Z:A \quad r:A\in\Theta}{\Gamma \vdash^{\Theta}_{c} \mathtt{set}\, r\, Z : \UU}
%     \end{mathpar}
%     By induction hypothesis,
%     \[ \mu(\Gamma,P)\vdash^{\Xi}_{v} Z:\mu(A,P). \]
%     Since $P$ does not mention $r$ by hypothesis ($\mathcal{R}(P) \cap \Theta = \emptyset$), substitution does not affect $P$. Thus Rule H-SET gives
%     \[ 
%         \mu(\Gamma,P) \vdash^{\Xi}_{c} \{P\}\, \mathtt{set}\, r\, Z \,\{\lambda\_.P\} : \UU. 
%     \]

%     \item Rule CASE:
%     \begin{mathpar}
%     \inferrule*[right=case]{\Gamma \vdash^{\Theta}_{v} Z:\BB \and \Delta \vdash^{\Theta}_{c} M:A \and \Delta \vdash^{\Theta}_{c} N:A}
%     {\Gamma\boxplus\Delta \vdash^{\Theta}_{c} \ifthen{Z}{M}{N}:A}
%     \end{mathpar}
%     By induction hypothesis,
%     \[ \mu(\Gamma,P)\vdash^{\Xi}_{v} Z:\BB \]
%     and
%     \[ 
%         \mu(\Delta,P)\vdash^{\Xi}_{c}\{P\}\, M \,\{\lambda v.P\}, \quad
%         \mu(\Delta,P)\vdash^{\Xi}_{c}\{P\}\, N \,\{\lambda v.P\}.
%     \]
%     Rule H-CASE yields
%     \[ \mu(\Gamma,P)\boxplus\mu(\Delta,P) \vdash^{\Xi}_{c} \{P\}\, \ifthen{Z}{M}{N} \,\{\lambda v.P\} : \mu(A,P).
%     \]

%     \item Rule FUN:
%     \begin{mathpar}
%     \inferrule*[right=fun]{\mathsf{typeof}(f)=G_1\times\dots\times G_m \to G
%     \quad (\Gamma_k p \vdashtheta{v} Z_k : G_k p)_{1\le k\le m}
%     \quad p \in \mathbb N_{\ge 1}[i]}{\boxplus_k \Gamma_k p \vdashtheta{c}
%     f_p(Z_1,\dots,Z_m): G p}
%     \end{mathpar}
%     By the induction hypothesis, for each $k$ we have
%     \[ \mu(\Gamma_k,P) \vdashtheta{v} Z_k: \mu(G_k p,P). \]
%     Since for every polynomial $p$, $\mu(G p,P)$ maps to $G p$ for ground types, Rule H-FUN yields
%     \[ \boxplus_k \mu(\Gamma_k,P) \vdashtheta{c} \{P\}\, f_p(Z_1,\dots,Z_m) \,\{\lambda v.P\} : \mu(G p,P).
%     \]
% \end{itemize}

% All typing rules are preserved, except for Rule~\textsf{LOOP}, which is outside the current scope. Therefore, we can conclude.
% \end{proof}

In addition to the introduction of Hoare-style reasoning in $\lamBLL$, we also present a set of equivalences between terms in the language $\lamBLL$ extended with Hoare triples. In particular, in Figure~\ref{fig:hl-equations} we define two equations.
\begin{figure}[htpb]
    \centering
    \begin{align*}
    \{P\}\, \mathtt{if}\, b\, \mathtt{then}\, &M \,\mathtt{else}\, N \,\{Q\} \sim \{P\}\, M \,\{Q\} \tag{ifTrue}\label{ifTrue} \\
    \text{if} \ P &\models b
    \\[2ex]
    \{P\}\, \mathtt{if}\, b\, \mathtt{then}\, &M \,\mathtt{else}\, N \,\{Q\} \sim \{P\}\, N \,\{Q\} \tag{ifFalse}\label{ifFalse} \\
    \text{if} \ P &\models \neg b
    \end{align*}
    % \begin{equation}
    %     \vcenter{\hbox{$
    %         \inferrule*[]{\Phi \models b}{\{\Phi\}\, \ifthen{b}{M}{N} \,\{Q\}}
    %     $}} \sim M \tag{ifTrue}
    % \end{equation}
    % \begin{equation}
    %     \vcenter{\hbox{$
    %         \inferrule*[]{\Phi \models \neg b}{\{\Phi\}\, \ifthen{b}{M}{N} \,\{Q\}}
    %     $}} \sim N \tag{ifFalse}
    % \end{equation}
    \caption{Equations in $\lamBLL$ with Hoare extension.}
    \label{fig:hl-equations}
\end{figure}

The two equations state that, whenever the precondition entails the guard of a conditional, the conditional term collapses to the corresponding branch, since the alternative branch is unreachable under the given precondition. Under the precondition $P$ such that $P \models b$, the behaviour of the conditional term $\ifthen{b}{M}{N}$ is equivalent to that of $M$, since the guard is always true and the branch $N$ is never executed. Symmetrically, if $P \models \neg b$, the effect of the conditional is equivalent to that of $N$, as the guard is always false and the branch $M$ becomes unreachable.