\chapter{Formal Verification Tools in the Computational Model}
\lhead[\fancyplain{}{\bfseries\thepage}]{\fancyplain{}{\bfseries\rightmark}}

Automated proof systems are machine-checked frameworks that enable cryptographers to reason directly within the computational model or the symbolic model while providing a formal and machine-verifiable environment for proving the security of cryptographic protocols. While many such frameworks exist for the symbolic model of cryptography, comparatively little work has been done in the computational model, largely due to the complexity of probabilistic reasoning and reduction-based proofs. Over the last two decades, however, several tools have been developed to address this gap, including EasyCrypt \cite{easy-crypy-a-tutorial}, Squirrel \cite{the-squirrel-prover-and-its-logic} and CryptoVerif \cite{a-computationally-sound-automatic-prover-for-cryptographic-protocols}. EasyCrypt
performs Hoare-style proofs of programs, CryptoVerif relies on game transformations while Squirrel reasons over execution traces of protocols.

\section{EasyCrypt}
In EasyCrypt, security goals and hardness assumptions are modeled as probabilistic programs, called experiments (or games), which include unspecified adversarial code. EasyCrypt supports common reasoning patterns from the game-based approach, in which reductionist security proofs are decomposed into a sequence (or, more generally, a tree) of transformations called \textit{game hops}. Each hop introduces a local change to the experiment whose impact on the adversary’s distinguishing advantage can be formally bounded, making the overall proof easier to understand, structure, and check. Since each game hop relates two probabilistic programs, EasyCrypt relies on probabilistic relational Hoare logic (pRHL) to reason about pairs of programs. pRHL defines judgments of the form
\[
[c_1 \sim c_2 : \Phi \Longrightarrow \Psi]
\]

where $c_1$ and $c_2$ are probabilistic programs, and $\Phi$ and $\Psi$ are relational assertions on two memories. An example is $x\langle1\rangle = x\langle2\rangle$, which states that the value of $x$ coincides in both memories. pRHL judgments do not explicitly refer to probabilities but probabilistic statements can still be derived from valid judgments. The semantics of pRHL is based on a notion of \textit{lifting} which allows one to derive equalities and inequalities between probabilities of events in related programs. For instance, from pRHL judgment of the form
\[
[c_1 \sim c_2 : \Phi \Longrightarrow E\langle1\rangle \to F\langle2\rangle]
\]
one can derive that
\[
\mathrm{Pr}[c_1, m_1: E] \leq \mathrm{Pr} [c_2, m_2: F ]
\]
for all initial memories $m_1$ and $m_2$ that are related by $\Phi$, and for events $E$ and $F$.

A concrete example illustrating the use of pRHL involves reasoning about two probabilistic experiments (games), shown in Figure \ref{fig:easycrypt-sample-proof}. The goal is to prove that the two games induce the same joint distribution over pairs $(m,c)$. This captures the perfect secrecy of the one-time pad, the ciphertext produced by encrypting a message with a uniformly random key is itself uniformly random and independent of the message. 

\begin{figure}[htpb]
    \centering
    \begin{minipage}[t]{0.25\textwidth}
    \begin{align*}
    &\mathtt{OTP}(n): \\
    &\quad m \gets \mathcal{M} \\
    &\quad k \gets \{0,1\}^n \\
    &\quad c \gets m \oplus k \\
    &\quad \mathtt{return} (m,c)
    \end{align*}
    \end{minipage}
    \hspace{2em}
    \begin{minipage}[t]{0.25\textwidth}
    \begin{align*}
    &\mathtt{Uniform}(n): \\
    &\quad m \gets \mathcal{M} \\
    &\quad c \gets \{0,1\}^n \\
    &\quad \mathtt{return} (m,c)
    \end{align*}
    \end{minipage}
    \caption{Example of $\mathtt{OTP}$ and $\mathtt{Uniform}$ cryptographic games.}
    \label{fig:easycrypt-sample-proof}
\end{figure}
In pRHL, this equivalence can be expressed by the judgment
\[
[\mathtt{OTP} \sim \mathtt{Uniform}: \Phi \Longrightarrow m\langle1\rangle = m\langle2\rangle \land c\langle1\rangle = c\langle2\rangle ]
\]
where the precondition 
$\Phi$ relates the initial memories of the two games. In this case, since both programs start from empty memories and no inputs are assumed, 
can simply be taken as $(\Phi = \top)$. By the soundness of pRHL, a judgment of the form
\[[\mathtt{OTP} \sim \mathtt{Uniform}: \Phi \Longrightarrow res\langle1\rangle = res\langle2\rangle] \Longrightarrow \mathtt{OTP} \equiv \mathtt{Uniform}
\]
implies that the two programs are observationally equivalent. To establish the judgment, we proceed as follows. Both programs begin by sampling a message $m$ using the same distribution $\mathcal{M}$ which is assumed to be the set containing all the bit strings of length $n$. Therefore, we can couple the two samplings so that  $m\langle1\rangle=m\langle2\rangle$. Next the $\mathtt{OTP}$ game samples a key $k \gets \{0,1\}^n$ uniformly at random, while the $\mathtt{Uniform}$ game samples a ciphertext $c \gets \{0,1\}^n$ uniformly at random. We define a coupling between these samplings by setting
\[
c\langle2\rangle = m \oplus k
\]
and this is valid because, for any fixed message $m$
m, the distribution of  $m\oplus k$ is uniform whenever $k$ is uniformly distributed. Finally, the $\mathtt{OTP}$ game sets
\[
c\langle1\rangle = m \oplus k
\]
which implies $c\langle1\rangle = c\langle2\rangle$. The postcondition 
\[
(m\langle1\rangle = m\langle2\rangle) \land (c\langle1\rangle = c\langle2\rangle)
\]
holds and we conclude that
\[
\mathtt{OTP} \equiv \mathtt{Uniform}
\]
This shows that pRHL can formally capture standard cryptographic arguments by reasoning about couplings between probabilistic programs rather than directly manipulating probability distributions.

Additionally in pRHL, given a valid judgment of the form
\[
\left[ c_1 \sim c_2: \Phi \Longrightarrow \bigwedge\limits_{i=1}^n x_i\langle1\rangle = x_i\langle2\rangle \right]
\]
it holds that $\mathrm{Pr}[c_1, m_1: A] = \mathrm{Pr}[c2, m_2: A]$ for every initial memories $m_1$ and $m_2$ that are related by $\Phi$ and event $A$ that only depends on $\{x_1, \dots , x_n\}$. A further generalization of observational equivalence is observational equivalence up to a
failure event $F$
\[
\left[ c_1 \sim c_2: \Phi \Longrightarrow \neg F\langle2\rangle \to \bigwedge\limits_{i=1}^n x_i\langle1\rangle = x_i\langle2\rangle \right] 
\]
And it follows that from the above judgment that for every initial memories $m_1$ and $m_2$ that are related by $\Phi$ and event $A$ that only depends on $\{x_1, \dots , x_n\}$ it holds
\[
\mathrm{Pr}[c_1, m_1: A] \leq Pr [c_2, m_2 : A] + \mathrm{Pr}[c_2, m_2 : F] 
\]
This is useful to take into account upper bounds on the probability of some event (typically a failure).

The second key component of EasyCrypt is a probabilistic Hoare Logic (pHL) that enables probabilistic reasonging about certain events in a game. Judgments in pHL have the form
\[ 
[c : \varsigma \Longrightarrow \varphi] \diamond p \]
where $c$ is a probabilistic program, $\varsigma$ and $\varphi$ are (non-relational) assertions, $\diamond$ is a comparison operator and $p$ is a probability expression.

In EasyCrypt, both pRHL and pHL are embedded into a higher-order logic, and reasoning within this logic is supported by a core proof engine inspired by SsReflect, an extension of Coq \cite{ssreflect-coq}. To support compositional reasoning, which is essential when analyzing cryptographic systems, as they are typically constructed by composing multiple cryptographic primitives, EasyCrypt provides a module system. It enables the development of modular proofs that reflect the compositional structure of cryptographic constructions. In addition, it make easier the reasoning, organization and management of large and complex proofs involving numerous game hops and reductions at different levels of abstraction.

\section{Squirrel}
Squirrel is a proof assistant tool that enables automated verification techniques for the computational model. The system is based on the Computationally Complete Symbolic Attacker (CCSA) approach, which retains a symbolic reasoning framework while avoiding some of the limitations of traditional symbolic models. Rather than modeling an adversary's capabilities by explicitly specifying all actions the adversary can perform, the CCSA approach characterizes the adversary by specifying what it cannot do. Starting from the assumed security properties of cryptographic primitives, one derives inference rules that express which pairs of message sequences are computationally indistinguishable. Rules are proven sound with respect to an interpretation of terms as Probabilistic Polynomial-Time Turing Machines (PPTM). Any security proof carried out using the rules implies security in the computational model, given that the underlying cryptographic assumptions hold.

The CCSA approach reasons about cryptographic protocols viewed as concurrent programs (or games) that rely on cryptographic primitives while executing in a malicious environment. A central concept in this setting is computational indistinguishability: two games are said to be indistinguishable if any probabilistic polynomial-time adversary has at most a negligible probability of distinguishing between them. The CCSA framework uses first-order logic to establish computational indistinguishability by first modeling the messages generated during a game’s execution as first-order terms, and then reasoning about indistinguishability within first-order logic using inference rules derived from the assumed security properties of the underlying cryptographic primitives. Describing cryptographic games as probabilistic imperative programs allows cryptographers to rely on an intuitive understanding of their semantics and avoids the introduction of overly complex execution models. Formally reasoning about such programs can be challenging, as it requires handling statefulness, loop invariants, and probabilistic dependencies. Moreover, while encoding complex protocols as imperative programs is possible, it is unnatural and can significantly complicate security proofs.

The CCSA approach represents only the messages computed during a game, modeling them as first-order terms. These terms are pure in the functional programming sense and are therefore easier to reason about than full game descriptions that maintain mutable state. To illustrate this idea, let us consider a simple game.
\begin{figure}[htpb]
    \centering
    \begin{align*}
    &\mathtt{G}(n): \\
    &\quad k \gets \{0,1\}^n \\
    &\quad m \gets A() \\
    &\quad t \gets Enc_k(m) \\
    &\quad x \gets A(t) \\
    &\quad\mathtt{return}\,\, x
    \end{align*}
    \caption{Example of a cryptographic game $\mathtt{G}$ to discuss a Squirrel proof.}
    \label{fig:squirrel-sample-proof}
\end{figure}

A secret key $k$ of length $n$ (Squirrel normally uses $\eta$ instead of $n$) is randomly sampled. The attacker $A$ is asked to provide a message $m$ which is then encrypted using $k$ and sent back to $A$, which uses it to return a second message $x$ which is the final output of the game.

The sampling of the secret key can be modeled by a name symbol $k$. The first interaction with the adversary $A$ is represented by the adversarial function symbol $\mathtt{att}_0(\cdot)$. Although the encryption function $Enc_k$ may be probabilistic may be probabilistic, Squirrel explicitly tracks probabilistic dependencies by modeling the randomness used by the encryption algorithm. This is achieved by introducing a fresh name symbol $\mathtt{r}$ and representing the encryption as the term $\mathtt{enc(att_0(\cdot),r,sk)}$. The second interaction with the adversary is then modeled by another adversarial function symbol, for instance $\mathtt{att_1(enc(att_0(\cdot),r,sk))}$. This representation models two successive calls to the stateful adversary $A$ in the game using two pure function symbols $\mathtt{att_0}$ and $\mathtt{att_1}$. Any state computed by the adversary during the first interaction and used in the second can be recomputed when modeling the second interaction as a pure function. This allows CCSA to avoid explicit state while preserving the adversary's capabilities.

n Squirrel, terms are parameterized by a security parameter $\eta \in \mathbb{N}$ and by sources of randomness provided by a pair of random tapes $\rho=(\rho_h,\rho_a)$, each filled with random bits. A model $\mathbb{M}$ of Squirrel's logic provides the following interpretations:
\begin{itemize}
    \item For each honest function symbol $\mathtt{f}$ its interpretation as PPTM $\mathcal{M}_\mathtt{f}$
    \item Associate to each name $\mathtt{n}$ a unique sub-sequence of $\eta$ bits in $\rho_h$ using a PPTM $\mathcal{M}_\mathtt{n}$ with access to $\rho_h$, such that distinct names use disjoint parts of $\rho_h$;
    \item Interpret any adversarial function symbol $\mathtt{att}$ as a PPTM $\mathcal{M}_\mathtt{att}$ which can only access the random tape $\rho_a$ and not $\rho_h$.
\end{itemize}
A term $\mathtt{t}$ is interpreted as a function that associates to each value of the security parameter $\eta$ and random tapes $\rho$, a concrete value denoted by $[\![t]\!]_\mathbb{M}^{\eta, \rho}$. The mapping $\eta, \rho \mapsto [\![t]\!]_\mathbb{M}^{\eta, \rho}$ is computable by a PPTM.

As one of the most commonly used notions to define cryptographic properties is computational indistinguishability, Squirrel's logic relies on the symbol $\sim$ to represent it. Formally, for two terms $\mathtt{u}$, $\mathtt{v}$, the predicate $\mathtt{u} \sim \mathtt{v}$ holds in a model $\mathbb{M}$ if $\mathtt{G}^\mathbb{M}_\mathtt{u} \approx \mathtt{G}^\mathbb{M}_\mathtt{v}$, and $\mathtt{G}^\mathbb{M}_\mathtt{t}$ is the game where the adversary is provided with $[\![t]\!]_\mathbb{M}^{\eta, \rho}$ and must produce a bit b, returned by the game. $\mathtt{u}$, $\mathtt{v}$ holds in $\mathbb{M}$ if exists a negligible function $\epsilon$ such that
\begin{align*}
\forall A \in \mathrm{PPTM}. \eta \mapsto |\mathrm{Pr}_\rho(A([\![u]\!]_\mathbb{M}^{\eta, \rho},\rho_a)=1) - \mathrm{Pr}_\rho(A([\![v]\!]_\mathbb{M}^{\eta, \rho},\rho_a)=1)| \leq \epsilon(\eta)
\end{align*}
Let's consider another game $\mathtt{G}_\mathtt{k}$ reported in Figure \ref{fig:squirrel-sample-proof-2}.
\begin{figure}[htpb]
    \centering
    \begin{align*}
    &\mathtt{G}_\mathtt{k}(\eta): \\
    &\quad k \gets \{0,1\}^{\eta} \\
    &\quad m \gets A() \\
    &\quad \mathtt{if}\,\, m = sk\,\,\mathtt{then} \\
    &\quad\quad \mathtt{return}\,\,0 \\
    &\quad\mathtt{else} \\
    &\quad\quad \mathtt{return}\,\,1
    \end{align*}
    \caption{Example of a cryptographic game $\mathtt{G}_\mathtt{k}$ to discuss computational indistinguishability in Squirrel.}
    \label{fig:squirrel-sample-proof-2}
\end{figure}
The message returned at the end of the game can be represented by the term
\[
\phi_k = \mathtt{if}\,\,(\mathtt{att_0} = sk) \,\,\mathtt{then}\,\,0\,\,\mathtt{else}\,\,1
\]
which outputs $0$ if the adversary correctly guesses the secret key $k$ and $1$ otherwise. Checking the indistinguishability judgment $(\phi_k \sim 1)$ amounts to verify that the corresponding game $\mathtt{G}_\mathtt{k}$ is computationally indistinguishable from the trivial game $(\mathtt{return}\,\,1)$ i.e., $G_k \approx (\mathtt{return}\,\,1)$.

\begin{figure}[htpb]
    \centering
    \begin{gather*}
    \begin{prooftree}
    \hypo{}
    \infer1[Ref]{u \sim u}
    \end{prooftree}
    \quad\quad
    \begin{prooftree}
    \hypo{(u = v)\sim \mathtt{true} }
    \hypo{C[v]\sim w}
    \infer2[Rew]{C[u] \sim w}
    \end{prooftree}
    \quad\quad
    \begin{prooftree}
    \hypo{}
    \infer1[Fresh]{(t \neq \mathtt{n})\sim \mathtt{true}}
    \end{prooftree}
    \end{gather*}
    \caption{Inference rules over $\sim$ in Squirrel.}
    \label{fig:inference-rules-over-sim-squirrel}
\end{figure}

Given the inference rules over $\sim$ in Squirrel represented in Figure \ref{fig:inference-rules-over-sim-squirrel} one can derive the result as follows

\begin{gather*}
    \begin{prooftree}
    \hypo{}
    \infer1[Fresh]{(\mathtt{att}_0(\cdot) \neq k)\sim \mathtt{true}}
    \infer1{((\mathtt{att_0}(\cdot) = k) = \mathtt{false}) \sim \mathtt{true}}
    \hypo{}
    \infer1[Ref]{1\sim1}
    \infer1{(\mathtt{if}\,\,\mathtt{false}\,\,\mathtt{then}\,\,0\,\,\mathtt{else}\,\,1)\sim 1}
    \infer2[Rew]{(\mathtt{if}\,\,(\mathtt{att_0}(\cdot) = k) \,\,\mathtt{then}\,\,0\,\,\mathtt{else}\,\,1)\sim 1}
    \end{prooftree}
\end{gather*}

% The key rules is the second one that states that if given $u$ indistinguishable from $v$, then  replace $u$ can be replaced with $v$ inside any context $C$ without changing the overall indistinguishability from $w$.

\section{CryptoVerif}

% \begin{figure}
%     \centering
%     \begin{align*}
%     &\mathtt{PrivK}^{CPA}_{A,\Pi}(n): \\
%     &\quad k \gets \{0,1\}^n \\
%     &\quad m_0, m_1 \gets A^{Enc_k(\cdot)}(1^n) \\
%     &\quad b \gets \{0,1\} \\
%     &\quad c \gets Enc_k(m_b) \\
%     &\quad g \gets A(c, Enc_k(\cdot)) \\
%     &\quad \mathtt{return}\,\,(b=g)
%     \end{align*}
%     \caption{Pseudocode for $\mathtt{PrivK}^{CPA}$ Experiment}
%     \label{fig:placeholder}
% \end{figure}